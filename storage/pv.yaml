apiVersion: v1
kind: PersistentVolume
metadata:
  name: pv-log
  labels:
    name: pv-log
spec:
  storageClassName: local-path
  capacity:
    storage: 100Mi
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /pv/log
